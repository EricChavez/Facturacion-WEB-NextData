'use strict';
angular
  .module('softvApp')
  .controller('AplicacontratosCtrl', function ($uibModal, $rootScope, ngNotify, encuestasFactory) {

    function initialData() {
     
    }

    

    var vm = this;
    initialData();
  
  });
